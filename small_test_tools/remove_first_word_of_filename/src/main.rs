/* The programm change first word of a filename in a catalog (the first word means the string of
* characters up to the separate chacacters: ` `, `_`, `-`) */
/* Zmiana programu Zmień pierwsze słowo nazwy pliku w katalogu (pierwsze słowo oznacza ciąg* znaków do oddzielnych chacacterów: ``, `_`,` -`) */

use remove_first_word_of_filename::*;

fn main() {
    println!("Hello, world!");
}
